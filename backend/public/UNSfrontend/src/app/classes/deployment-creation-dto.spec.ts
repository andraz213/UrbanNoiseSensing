import {DeploymentCreationDTO} from './deployment-creation-dto';

describe('DeploymentCreationDTO', () => {
  it('should create an instance', () => {
    expect(new DeploymentCreationDTO()).toBeTruthy();
  });
});
